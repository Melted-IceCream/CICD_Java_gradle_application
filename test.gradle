test {
    useJUnitPlatform()
    testLogging {
        events "PASSED", "FAILED", "SKIPPED"
    }
}

// Create test report
jacocoTestReport {
    dependsOn test
    reports {
        xml.required = true
        html.required = true
    }
}
